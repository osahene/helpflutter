<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (plain http://) is allowed only for local-dev hosts — the
  Android emulator's loopback alias and a couple of common local-network
  forms. Everything else (including the production backend, which is
  https:// anyway) falls through to the base-config default of HTTPS-only.
  Replaces a previous blanket android:usesCleartextTraffic="true", which
  allowed cleartext to any host app-wide.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
